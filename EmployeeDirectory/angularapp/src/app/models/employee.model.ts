export class Employee {
    userId: string; // Assuming userId is a string, adjust the type accordingly
    employeeId?: string; // employeeId is optional as it will be generated on the server
    firstName: string;
    lastName: string;
    mobileNumber: string;
    mailId: string;
    dateOfBirth: string;
    age: string;
    gender: string;
    education: string;
    experience: string;
    photo: string;
}